<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.blive.orm.dao.LogQRCodeScanDao">

    <insert id="insertOne">
        INSERT IGNORE INTO bvision_log_qrcode_scan (user_id, ip)
        VALUES (#{userId}, #{ip})
    </insert>

</mapper>