<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.blive.orm.dao.FeedbackDao">

    <insert id="insertOne">
        INSERT IGNORE INTO bvision_feedback (user_id, subject, description)
        VALUES (#{userId}, #{subject}, #{description})
    </insert>

</mapper>