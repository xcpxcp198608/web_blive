<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.blive.orm.dao.RelationFriendDao">

    <select id="selectFriendsIdByUserId" resultType="java.lang.Integer">
        SELECT friend_id
        FROM relation_friend
        WHERE user_id = #{userId}
    </select>

    <insert id="insertOne">
        INSERT IGNORE INTO relation_friend (user_id, friend_id)
          VALUES (#{userId}, #{friendId})
    </insert>

    <delete id="deleteOne">
        DELETE IGNORE FROM relation_friend WHERE user_id = #{userId} AND friend_id = #{friendId}
    </delete>

</mapper>