<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.blive.orm.dao.LiveViewDao">

    <insert id="insertOne" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
      INSERT IGNORE INTO bvision_live_view (player_id, viewer_id, modify_time)
      VALUES (#{playerId}, #{viewerId}, now())
    </insert>
    
    <update id="updateOne">
        UPDATE IGNORE bvision_live_view SET modify_time = now() WHERE id = #{id}
    </update>

    <select id="selectBy2Id" resultType="com.wiatec.blive.orm.pojo.LiveViewInfo">
        SELECT * FROM bvision_live_view WHERE player_id = #{playerId} AND viewer_id = #{viewerId}
    </select>

</mapper>